# utils.py

def format_response(response):
    """Formats the response into a clean structure."""
    # Implement the response formatting logic
    return response.strip()

def handle_error(error):
    """Handles errors and returns a standardized error message."""
    return f"Error: {str(error)}"
